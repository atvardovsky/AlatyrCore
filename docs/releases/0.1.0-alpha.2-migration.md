# Alatyr Release Migration Report

This reviewed report records the source-tooling portability update after
`0.1.0-alpha.1`. It does not approve or apply changes in an installed target
adapter.

Report ID: `alatyr-0.1.0-alpha.2`
Prepared by: `AlatyrCore maintainers`
Prepared at: `2026-07-14`

## Version Scope

From source baseline: `v0.1.0-alpha.1`
To source baseline: release commit for `v0.1.0-alpha.2`
From manifest: `framework/rule-registry.json` at `v0.1.0-alpha.1`
To manifest: `framework/rule-registry.json` at `v0.1.0-alpha.2`
From framework version: `0.1.0-alpha.1`
To framework version: `0.1.0-alpha.2`
From adapter schema version: `1`
To adapter schema version: `1`
From template version: `2`
To template version: `2`

## Adapter Contract Impact

Framework version: `changed`
Adapter schema version: `unchanged`
Template version: `unchanged`
Rule registry: `unchanged`
Rule ownership: `unchanged`
Framework files: `unchanged`
Target template surfaces: `unchanged`

## Affected Rule Categories

- none; this release corrects source-repository portability checks

## Affected Task Profiles

- source tooling
- release versioning

## Affected Canonical Sources

- none

## Migration Action Hints

- Installed target adapters do not need a framework or template content
  migration for this release.
- Maintainers using source conformance tooling should update to this release
  for native Windows path normalization and macOS temporary-path handling.
- Use `recheck-after-framework-update` only when the target also changes its
  recorded framework baseline or consumes other unreconciled releases.

## Rule Changes

Added rules:

- none

Changed rules:

- none

Removed rules:

- none

Unchanged rules:

- all 13 registered Alatyr rules

## Rule Owner Changes

- none

## Framework File Changes

- none

## Target Template Surface Changes

- none

## Required Target Actions

- none for installed framework or adapter content
- update a recorded framework version only when the target intentionally
  adopts this source baseline

## Optional Target Actions

- rerun source or target-local tooling on native platforms relevant to the
  target project

## Approval Needs

Approval needed: `target-dependent only when changing an installed baseline`
Approval scope: `not required for source-tooling consumption alone`

## Validation Run

Source validation: `python3 tools/check_all.py` passed locally with 38 checks.
GitHub Actions run `29350257731` passed the same suite on native Ubuntu, macOS,
and Windows runners.

Target validation: not required for unchanged portable framework and template
content; target projects retain ownership of local validation.

## Residual Risks

- GitHub-hosted runner success does not cover every Python or operating-system
  version used by downstream maintainers.
- Non-Codex assistant execution evidence remains external to this
  source-tooling portability release.

## Safety

This report is evidence only. It does not modify target files, approve changes,
or complete an installed adapter upgrade.
