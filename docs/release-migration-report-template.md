# Alatyr Release Migration Report

Use this template when preparing source release evidence for an AlatyrCore
framework update. This is not a target adapter migration note and does not
approve or apply target changes.

Report ID: `{RELEASE_MIGRATION_REPORT_ID}`
Prepared by: `{PREPARED_BY}`
Prepared at: `{PREPARED_AT}`

## Version Scope

From manifest: `{FROM_RULE_REGISTRY_PATH}`
To manifest: `{TO_RULE_REGISTRY_PATH}`
From framework version: `{FROM_FRAMEWORK_VERSION}`
To framework version: `{TO_FRAMEWORK_VERSION}`
From adapter schema version: `{FROM_ADAPTER_SCHEMA_VERSION}`
To adapter schema version: `{TO_ADAPTER_SCHEMA_VERSION}`
From template version: `{FROM_TEMPLATE_VERSION}`
To template version: `{TO_TEMPLATE_VERSION}`

## Adapter Contract Impact

Framework version: `{CHANGED_UNCHANGED_OR_NOT_COMPARED}`
Adapter schema version: `{CHANGED_UNCHANGED_OR_NOT_COMPARED}`
Template version: `{CHANGED_UNCHANGED_OR_NOT_COMPARED}`
Rule registry: `{CHANGED_UNCHANGED_OR_NOT_COMPARED}`
Rule ownership: `{CHANGED_UNCHANGED_OR_NOT_COMPARED}`
Framework files: `{CHANGED_UNCHANGED_OR_NOT_COMPARED}`
Target template surfaces: `{CHANGED_UNCHANGED_OR_NOT_COMPARED}`

## Affected Rule Categories

- `{AFFECTED_RULE_CATEGORY_OR_NONE}`

## Affected Task Profiles

- `{AFFECTED_TASK_PROFILE_OR_NONE}`

## Affected Canonical Sources

- `{AFFECTED_CANONICAL_SOURCE_OR_NONE}`

## Migration Action Hints

- `{MIGRATION_ACTION_HINT_OR_NONE}`

## Rule Changes

Added rules:

- `{ADDED_RULE_ID_OR_NONE}`

Changed rules:

- `{CHANGED_RULE_ID_OR_NONE}`

Removed rules:

- `{REMOVED_RULE_ID_OR_NONE}`

Unchanged rules:

- `{UNCHANGED_RULE_ID_OR_NONE}`

## Rule Owner Changes

Added rule owner categories:

- `{ADDED_RULE_OWNER_CATEGORY_OR_NONE}`

Changed rule owner categories:

- `{CHANGED_RULE_OWNER_CATEGORY_OR_NONE}`

Removed rule owner categories:

- `{REMOVED_RULE_OWNER_CATEGORY_OR_NONE}`

## Framework File Changes

Added framework files:

- `{ADDED_FRAMEWORK_FILE_OR_NONE}`

Changed framework files:

- `{CHANGED_FRAMEWORK_FILE_OR_NONE}`

Removed framework files:

- `{REMOVED_FRAMEWORK_FILE_OR_NONE}`

## Target Template Surface Changes

Added target template surfaces:

- `{ADDED_TARGET_TEMPLATE_SURFACE_OR_NONE}`

Changed target template surfaces:

- `{CHANGED_TARGET_TEMPLATE_SURFACE_OR_NONE}`

Removed target template surfaces:

- `{REMOVED_TARGET_TEMPLATE_SURFACE_OR_NONE}`

## Required Target Actions

- `{REQUIRED_TARGET_ACTION_OR_NONE}`

## Optional Target Actions

- `{OPTIONAL_TARGET_ACTION_OR_NONE}`

## Approval Needs

Approval needed: `{YES_NO_REASON}`
Approval scope: `{APPROVAL_SCOPE_OR_NOT_REQUIRED}`

## Validation Run

Source validation: `{SOURCE_VALIDATION_RUN_OR_SKIPPED}`
Target validation: `{TARGET_VALIDATION_EXPECTATION_OR_UNRESOLVED}`

## Residual Risks

- `{RESIDUAL_RISK_OR_NONE}`

## Safety

This report is evidence only. It does not modify target files, approve
changes, or complete an installed adapter upgrade.
