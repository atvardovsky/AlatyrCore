# Shared Workspace Context

Use this directory only for compact support that genuinely applies across the
selected workspace modes for `{PROJECT_NAME}`.

Link to canonical repository purpose, workspace map, ownership, terminology,
or universal safety sources. Do not duplicate full project policies or place
mode-specific context here. Keep root context disabled when no shared support
is required.
