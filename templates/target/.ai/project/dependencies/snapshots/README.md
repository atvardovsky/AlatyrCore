# Dependency Knowledge Snapshots

Store only normalized dependency facts permitted by the target retention,
license, privacy, and repository-visibility policy.

Do not copy raw package documentation here by default. A snapshot must record
the exact package instance, export digest, review evidence, authority,
freshness, applicability, and retained fields. If policy selects `reference`
or `local-cache`, keep this directory empty except for this explanation.
