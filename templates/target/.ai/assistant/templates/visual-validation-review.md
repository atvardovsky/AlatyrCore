# Visual Validation Review

Operation: `{OPERATION_ID}`
Changed visual or diagram fact: `{CHANGED_FACT}`
Surface: `{UI_DIAGRAM_OR_ARTIFACT_SURFACE}`

Validation method:

- `{SCREENSHOT_TEST_RENDER_OR_MANUAL_REVIEW}`

Evidence:

- `{EVIDENCE_PATH_COMMAND_OR_REVIEW_NOTE}`

Checked states or sizes:

- `{STATE_SIZE_OR_VIEWPORT}`

Skipped checks:

- `{SKIPPED_CHECK_AND_REASON}`

Residual visual risk:

- `{RESIDUAL_VISUAL_RISK}`
