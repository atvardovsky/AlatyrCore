# Alatyr Core Installation Plan: Core Template

Installation id: `ALATYR-YYYYMMDD-short-name`

Discovery category map: `installer/discovery-contract.json`

Use this template for the mandatory plan-and-approval stage. Load only the
selected sections of `installer/installation-plan-template.md` when target
evidence or the resolved capability composition requires detailed module
questions.

## Target And Authority

- Target repository and revision:
- New installation or update:
- Primary stack and repository structure:
- Existing assistant instructions and supported assistant surfaces:
- Existing Alatyr manifest, version, ownership, and installation state:
- Selected support profile and framework pack:
- Selected, staged, deferred, and blocked optional modules:
- Current logical scope:
- Current user authorization (`inspect`, `modify`, `commit`, `publish`, or
  `live-external`) and exact source message:
- Prior authorization invalidated by a completed task, topic switch, changed
  plan, or changed target revision:

## Goal And Non-Goals

- Target outcome:
- Explicit non-goals:
- Protected categories and excluded actions:
- Missing or contradictory target facts:

Start with the `kernel` profile unless target evidence or the current request
justifies expansion. Scaffolding does not enable an optional capability.

## Core Target Evidence

- Product purpose and architecture areas:
- Canonical project sources and decision owners:
- Project contour, source-of-truth registry, and relevant relationships:
- Existing validation commands and CI evidence:
- Security, credential, privacy, destructive-operation, and live-service
  boundaries:
- Target adapter owner, backup owner, review cadence, and ownership map:
- Existing project-specific AI infrastructure and access policy:
- Local deviations and known gaps:

## Planned Surfaces

- Portable framework pack and projected rule registry:
- Target project-adapter files to create, preserve, or update:
- Assistant bridge files selected from verified client capabilities:
- Existing files that must remain untouched:
- Rejected source-repository facts, commands, fixtures, and policies:
- Optional capability sections loaded from the detailed template and why:
- Core session-continuity surfaces and ignored runtime storage:

## Validation And Evidence

- Structural adapter checks:
- Target-native checks discovered from repository evidence:
- Logical integrity and affected relationship review:
- Approval-record and actual-diff scope validation when required:
- Installation or update state transition evidence:
- Post-install or post-update assistant handoff:
- Skipped checks and residual risks:

## Approval Disposition

- Protected change present:
- Approval record or explicit non-protected rationale:
- Plan version and digest:
- Allowed files and surfaces:
- Excluded actions:
- Approval validity after target or plan changes:

Do not apply changes before current-scope modification authorization. Commit,
publish, and live-external actions remain separate authorization phases.
