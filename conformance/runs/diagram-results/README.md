# Diagram Operation Results

Store reviewed `diagram-discussion` result JSON by run ID and assistant surface
under this directory. Stable source checks validate the result template only;
actual client runs are external evidence and are required only when explicitly
requested with `--require-all-surfaces`.

Do not record secrets, private project facts, or raw conversations here.
